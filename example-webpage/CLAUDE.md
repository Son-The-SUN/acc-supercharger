# Example Webpage Snapshots

User-downloaded saves of live ACC Admin pages, used as DOM/CSS reference when writing chrome-extension content scripts.

## Saved Pages

- **Members panel** — `aaron canubas - Members - Autodesk Account Administration.html` (+ `_files/` assets)
- **Projects list** — `Projects - Autodesk Account Administration.html` (+ `_files/` assets)

Match injected HTML/CSS to the element hierarchy and class names found in these snapshots.
